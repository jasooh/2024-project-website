// default '/' page

// custom components
import TypingText from "../components/TypingText";

export default function Default() {
    return (<TypingText>i like to code stuff and cry over the job market.</TypingText>)
}